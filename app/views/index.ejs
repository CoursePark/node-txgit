<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<title>node-txgit</title>
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.0/css/bootstrap.min.css">
		<script src="//cdnjs.cloudflare.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
		<script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.0/js/bootstrap.min.js"></script>
	</head>
	<body>
		<nav class="navbar navbar-default" role="navigation">
			<div class="container-fluid">
				<div class="text-center">
					<h1>node-txgit</h1>
				</div>
			</div>
		</nav>
		
		<div class="container">
			<div class="row">
				<div class="col-md-6">
					<h4>
						<span class="label label-success">Success</span>
						<div class="visible-xs">
							<br>
						</div>
						App is running
					</h4>
				</div>
				<div class="col-md-6">
					<h4><small>Great, the app is running and available at <span class="label label-default"><%= host %></span>!</small></h4>
				</div>
			</div>
			
			<hr>
			
			<div class="row">
				<div class="col-md-6">
					<h4>
						<% if (GIT_USERNAME) { %>
							<span class="label label-success">Success</span>
						<% } else { %>
							<span class="label label-danger">Missing</span>
						<% } %>
						<div class="visible-xs">
							<br>
						</div>
						GIT_USERNAME
					</h4>
				</div>
				<div class="col-md-6">
					<% if (GIT_USERNAME) { %>
						<h4><small>Set to <span class="label label-default"><%= GIT_USERNAME %></span>.</small></h4>
					<% } else { %>
						<h4><small>You still need to set the <span class="label label-default">GIT_USERNAME</span> ENV variable.</small></h4>
					<% } %>
				</div>
			</div>
			
			<div class="row">
				<div class="col-md-6">
					<h4>
						<% if (GIT_PASSWORD) { %>
							<span class="label label-success">Success</span>
						<% } else { %>
							<span class="label label-danger">Missing</span>
						<% } %>
						<div class="visible-xs">
							<br>
						</div>
						GIT_PASSWORD
					</h4>
				</div>
				<div class="col-md-6">
					<% if (GIT_PASSWORD) { %>
						<h4><small>ENV variable is set, but it's a secret!</small></h4>
					<% } else { %>
						<h4><small>You still need to set the <span class="label label-default">GIT_PASSWORD</span> ENV variable.</small></h4>
					<% } %>
				</div>
			</div>
			
			<div class="row">
				<div class="col-md-6">
					<h4>
						<% if (GIT_REPO_URL) { %>
							<span class="label label-success">Success</span>
						<% } else { %>
							<span class="label label-danger">Missing</span>
						<% } %>
						<div class="visible-xs">
							<br>
						</div>
						GIT_REPO_URL
					</h4>
				</div>
				<div class="col-md-6">
					<% if (GIT_REPO_URL) { %>
						<h4><small>Set to <span class="label label-default"><%= GIT_REPO_URL %></span>.</small></h4>
					<% } else { %>
						<h4><small>You still need to set the <span class="label label-default">GIT_REPO_URL</span> ENV variable.</small></h4>
					<% } %>
				</div>
			</div>
			
			<hr>
			
			<div class="row">
				<div class="col-md-6">
					<h4>
						<% if (clone) { %>
							<span class="label label-success">Success</span>
						<% } else if (cloneError) { %>
							<span class="label label-danger">Error</span>
						<% } else { %>
							<span class="label label-warning">In Progress</span>
						<% } %>
						<div class="visible-xs">
							<br>
						</div>
						Clone Repo Status
					</h4>
				</div>
				<div class="col-md-6">
					<% if (clone) { %>
						<h4><small>Repo has successfully been cloned.</small></h4>
					<% } else if (cloneError) { %>
						<h4><small>There was an error trying to clone <span class="label label-default"><%= GIT_REPO_URL %></span>.</small></h4>
						<pre><%= cloneError %></pre>
					<% } else { %>
						<h4><small>The repo cloning is currently in progress. <a href="#" onclick="location.reload()">Refresh to update status.</a></small></h4>
					<% } %>
				</div>
			</div>
			
			<hr>
			
			<div class="row">
				<div class="col-xs-12 text-center">
					<p class="text-muted">
						Powered by <a href="https://github.com/coursepark/node-txgit">node-txgit</a><br>
					</p>
				</div>
			</div>
		</div>
	</body>
</html>
